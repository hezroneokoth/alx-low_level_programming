#include <stdio.h>

/**
 * main - Prints all single numbers of base 10
 * starting with 0
 * followed by a new line.
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int i;

	for (i = 0; i <= 9; i++)
	{
		printf("%d\n", i);
	}
	return (0);
}
